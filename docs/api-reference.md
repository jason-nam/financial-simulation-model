# API Reference

## finmodel Package

### Classes

#### Asset

```python
from finmodel import Asset
```

**Note:** This class is currently a stub and raises `NotImplementedError` for all methods.

##### Methods

- `__init__(*args, **kwargs)`: Initialize an Asset
- `value() -> float`: Calculate the current value of the asset
- `update(*args, **kwargs)`: Update the asset state

#### Portfolio

```python
from finmodel import Portfolio
```

**Note:** This class is currently a stub and raises `NotImplementedError` for all methods.

##### Methods

- `__init__(*args, **kwargs)`: Initialize a Portfolio
- `add_asset(*args, **kwargs)`: Add an asset to the portfolio
- `remove_asset(*args, **kwargs)`: Remove an asset from the portfolio
- `total_value() -> float`: Calculate the total value of the portfolio

## Implementation Status

All classes and methods are currently stubs that raise `NotImplementedError`. Full implementation is planned for future releases.
